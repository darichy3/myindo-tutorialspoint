jar {
    manifest {
        attributes(
                "Created-By": "Darichy",
                "Specification-Title": "Tutorialspoint",
                "Main-Class" : "id.myindo.tutorialspoint.regex.core.RegularExpression",
                "Class-Path": configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}

